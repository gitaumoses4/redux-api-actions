/**
 * This are private action types reserved for the redux api actions.
 * @param action
 * @constructor
 */

const INITIALIZE = (action: string) => `${action}_316ftzfgq7_INIT`

const SUCCESS = (action: string) => `${action}_dbgucgo7t4_SUCCESS`

const FAILURE = (action: string) => `${action}_sn72a9bcnf_FAILURE`

const CLEAR = (action: string) => `${action}_pzcr5swlxv_FAILURE`

export default {
  INITIALIZE,
  SUCCESS,
  FAILURE,
  CLEAR
}
